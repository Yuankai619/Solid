import { createTheme } from '@mui/material/styles';

const TitleTheme = createTheme({
  typography: {
    fontFamily: [
      'Poppins',
      'sans-serif',
    ].join(','),
  },
  
});

export default TitleTheme;
